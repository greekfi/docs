# API Reference

This page is auto-generated from the Solidity source in
[`greekfi/protocol`](https://github.com/greekfi/protocol). It is
overwritten on every contract change by the `regenerate-api-reference`
workflow in protocol, which opens a PR against this repo.

The placeholder you're reading exists so the Docusaurus build doesn't
fail on the first deploy. It will be replaced with the real reference
the next time contracts change.
