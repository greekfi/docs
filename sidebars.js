// @ts-check

// The docs are a single long page; navigation is the page's own table of contents.
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {};

export default sidebars;
