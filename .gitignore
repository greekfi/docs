# Dependencies
/node_modules

# Production
/build

# Include lockfile here (the repo root's .gitignore excludes package-lock.json)
!/package-lock.json

# Generated files
.docusaurus
.cache-loader

# docs/api.md is auto-generated from greekfi/protocol's Solidity NatSpec
# but IS committed here — it's contributed via PRs from protocol's
# regenerate-api-reference workflow on contract changes. Committing
# means this repo can build and deploy without needing protocol checked
# out alongside it.

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
